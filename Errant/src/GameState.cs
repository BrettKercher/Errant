using Errant.src.Controllers;
using Errant.src.GameObjects;
using System;

namespace Errant.src {
    class GameMode {
        Type playerControllerClass = typeof(PlayerController);
        Type playerEntityClass = typeof(Player);
    }
}
